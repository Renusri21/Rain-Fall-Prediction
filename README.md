# Rain-Fall-Prediction
This project predicts rainfall (Yes/No) using a neural network trained on weather data like temperature, humidity, and wind speed. It includes training, prediction, and deployment using a Flask API. The model achieves around 70% accuracy and demonstrates a complete machine learning pipeline.
